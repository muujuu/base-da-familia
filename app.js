
const CONFIG={SUPABASE_URL:"https://ukpbnqzsthxywgabpumj.supabase.co",SUPABASE_ANON_KEY:"sb_publishable_3w3Z8dhj2zjDB7Yl2AB5RA_0eHzqL2a"},FAMILY_ID="base-mu-ju";let sb=null,remoteReady=false,savingRemote=false,missionView="base",taskModalType="daily",editingTaskId=null,calendarMonth=new Date(new Date().getFullYear(),new Date().getMonth(),1),trophyFilter="Todas";let state={tasks:JSON.parse(localStorage.getItem("bf_tasks")||"[]"),completions:JSON.parse(localStorage.getItem("bf_completions")||"[]"),checks:JSON.parse(localStorage.getItem("bf_checks")||"{}"),daySnapshots:JSON.parse(localStorage.getItem("bf_day_snapshots")||"{}"),goals:JSON.parse(localStorage.getItem("bf_goals")||"[]"),rewardsUsed:JSON.parse(localStorage.getItem("bf_rewards_used")||"[]")};const catIcon={casa:"🏠",pets:"🐾",saude:"🧠"},catLabel={casa:"Casa",pets:"Pets",saude:"Saúde"},weekNames=["Dom","Seg","Ter","Qua","Qui","Sex","Sáb"],weekFull=["Domingo","Segunda","Terça","Quarta","Quinta","Sexta","Sábado"],beerLevels=[["Bavaria","Início"],["Skol","Base ativa"],["Brahma","Rotina firme"],["Original","Guardião"],["Heineken","Especialista"],["Eisenbahn","Veterano"],["Baden Baden","Mestre"],["Patagonia","Comandante"],["Stella Artois","Arquiteto"],["Colorado","Lenda"],["Hoegaarden","Supremo"]],diff={facil:[10,"Fácil"],media:[25,"Média"],dificil:[50,"Difícil"],muito_dificil:[100,"Muito difícil"]},homeStages=[["🏚️","Base improvisada",0],["🏠","Casa organizada",750],["🏡","Lar confortável",2e3],["🌳","Base equilibrada",4500],["✨","Base lendária",8500],["🏰","Base suprema",14e3]],avatarItems=[["👕","Camiseta",250],["🧢","Boné",500],["👓","Óculos",1e3],["🧥","Moletom",2e3],["👑","Coroa",5e3],["✨","Aura",9e3]];function $(id){return document.getElementById(id)}function uid(){return Date.now().toString()+Math.random().toString(16).slice(2)}function dateKey(d){return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}function parseDate(k){return new Date(k+"T00:00:00")}function addDays(d,n){const x=new Date(d);x.setDate(x.getDate()+n);return x}function todayKey(){return dateKey(new Date)}function money(v){return Number(v||0).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}function localSave(){localStorage.setItem("bf_tasks",JSON.stringify(state.tasks));localStorage.setItem("bf_completions",JSON.stringify(state.completions));localStorage.setItem("bf_checks",JSON.stringify(state.checks||{}));localStorage.setItem("bf_day_snapshots",JSON.stringify(state.daySnapshots||{}));localStorage.setItem("bf_goals",JSON.stringify(state.goals));localStorage.setItem("bf_rewards_used",JSON.stringify(state.rewardsUsed||[]))}function migrate(){state.tasks.forEach(t=>{if(!t.scheduleType){t.scheduleType=t.frequency==="semanal"?"weekly":t.frequency==="mensal"?"monthly":"daily";t.interval=1;t.weeklyDays=t.scheduleType==="weekly"?[new Date().getDay()]:[];t.monthlyDay=t.scheduleType==="monthly"?new Date().getDate():1;t.estimatedMinutes=Number(t.time||5);t.startDate=t.startDate||todayKey();t.createdAt=t.createdAt||new Date().toISOString();t.active=t.active!==false;t.baseImpact=true}if(!["casa","pets","saude"].includes(t.category))t.category="casa"});if(state.completions.length===0&&state.checks)Object.entries(state.checks).forEach(([day,obj])=>Object.entries(obj).forEach(([taskId,ok])=>{const t=state.tasks.find(x=>x.id===taskId);if(ok&&t)state.completions.push(makeCompletion(t,day,true))}));localSave()}function setSync(t,txt){$("syncDot").className="dot "+t;$("syncText").textContent=txt}function applyData(d){state.tasks=d.tasks||[];state.completions=d.completions||[];state.checks=d.checks||{};state.daySnapshots=d.daySnapshots||{};state.goals=d.goals||[];state.rewardsUsed=d.rewardsUsed||[];migrate()}async function initSupabase(){if(!CONFIG.SUPABASE_URL||!CONFIG.SUPABASE_ANON_KEY||!window.supabase){setSync("local","modo local");return}sb=window.supabase.createClient(CONFIG.SUPABASE_URL,CONFIG.SUPABASE_ANON_KEY);try{const{data,error}=await sb.from("base_familia").select("data").eq("id",FAMILY_ID).maybeSingle();if(error)throw error;if(data&&data.data)applyData(data.data);else await sb.from("base_familia").upsert({id:FAMILY_ID,data:state,updated_at:new Date().toISOString()});remoteReady=true;setSync("online","sincronizado");sb.channel("base-familia-sync").on("postgres_changes",{event:"UPDATE",schema:"public",table:"base_familia",filter:`id=eq.${FAMILY_ID}`},p=>{if(savingRemote)return;if(p.new&&p.new.data){applyData(p.new.data);render(false)}}).subscribe()}catch(e){console.error(e);setSync("local","modo local")}}let saveTimer=null;function save(sync=true){localSave();if(sync&&remoteReady&&sb){clearTimeout(saveTimer);saveTimer=setTimeout(async()=>{savingRemote=true;setSync("online","salvando...");await sb.from("base_familia").upsert({id:FAMILY_ID,data:state,updated_at:new Date().toISOString()});savingRemote=false;setSync("online","sincronizado")},500)}}function calcXp(t){const base=(diff[t.difficulty||"facil"]||diff.facil)[0],min=Number(t.estimatedMinutes||5),tm=min>=60?2:min>=30?1.5:min>=15?1.2:min>=5?1.1:1,fm=t.scheduleType==="monthly"?1.7:t.scheduleType==="weekly"?1.25:1,cm=t.person==="Ambos"?1.25:1;return Math.round(base*tm*fm*cm)}function dueOnDate(t,date){if(t.active===false)return false;const start=parseDate(t.startDate||dateKey(new Date(t.createdAt||Date.now())));if(dateKey(date)<dateKey(start))return false;if(t.scheduleType==="daily"){const days=Math.floor((parseDate(dateKey(date))-start)/864e5);return days%Number(t.interval||1)===0}if(t.scheduleType==="weekly")return(t.weeklyDays||[]).map(Number).includes(date.getDay());if(t.scheduleType==="monthly")return date.getDate()===Number(t.monthlyDay||1);return false}function dueTasks(k){const d=parseDate(k);return state.tasks.filter(t=>dueOnDate(t,d))}function done(taskId,dueDate){return state.completions.some(c=>c.taskId===taskId&&c.dueDate===dueDate)}function makeCompletion(t,dueDate,legacy=false){return{id:uid(),taskId:t.id,taskText:t.text,category:t.category,person:t.person,xp:calcXp(t),baseImpact:t.baseImpact!==false,dueDate,completedAt:legacy?dueDate+"T12:00:00":new Date().toISOString()}}function completeTask(taskId,dueDate){const t=state.tasks.find(x=>x.id===taskId);if(!t||done(taskId,dueDate))return;state.completions.push(makeCompletion(t,dueDate));snapshot(dueDate);render()}function undoTask(taskId,dueDate){state.completions=state.completions.filter(c=>!(c.taskId===taskId&&c.dueDate===dueDate));snapshot(dueDate);render()}function snapshot(k){const due=dueTasks(k),d=due.filter(t=>done(t.id,k)).length;state.daySnapshots[k]={totalDue:due.length,done:d,percent:due.length?Math.round(d/due.length*100):0,perfect:due.length>0&&d===due.length}}function dayStats(k){if(state.daySnapshots[k])return state.daySnapshots[k];const due=dueTasks(k),d=due.filter(t=>done(t.id,k)).length;return{totalDue:due.length,done:d,percent:due.length?Math.round(d/due.length*100):0,perfect:due.length>0&&d===due.length}}function getInstances(from=-14,to=31){const arr=[],base=parseDate(todayKey());for(let i=from;i<=to;i++){const d=addDays(base,i),key=dateKey(d);dueTasks(key).forEach(t=>arr.push({task:t,dueDate:key,date:d,done:done(t.id,key),late:key<todayKey()&&!done(t.id,key)}))}return arr}function nextInstance(type=null){return getInstances(0,60).filter(i=>!i.done&&(!type||i.task.scheduleType===type)).sort((a,b)=>a.dueDate.localeCompare(b.dueDate)||calcXp(b.task)-calcXp(a.task))[0]}function labelDate(k){if(k===todayKey())return"Hoje";if(k<todayKey())return"Atrasada";const d=parseDate(k);return`${weekNames[d.getDay()]} ${d.toLocaleDateString("pt-BR",{day:"2-digit",month:"2-digit"})}`}function tagHtml(t,k){return`<div class="meta"><span class="tag">${catIcon[t.category]} ${catLabel[t.category]}</span><span class="tag">${t.person}</span><span class="tag">${t.estimatedMinutes||5} min</span><span class="tag">${calcXp(t)} XP</span><span class="tag">${labelDate(k)}</span></div>`}function missionCard(i){const t=i.task,el=document.createElement("div");el.className="mission-card";el.innerHTML=`<div><div class="mission-title">${catIcon[t.category]} ${t.text}</div>${tagHtml(t,i.dueDate)}</div><div class="actions">${i.done?`<button class="ghost small" onclick="undoTask('${t.id}','${i.dueDate}')">Desfazer</button>`:`<button class="green small" onclick="completeTask('${t.id}','${i.dueDate}')">Feita</button>`}<button class="small" onclick="openTaskModal('${t.scheduleType}','${t.id}')">Editar</button></div>`;return el}function renderMissions(){snapshot(todayKey());$("todayDate").textContent=new Date().toLocaleDateString("pt-BR",{weekday:"long",day:"2-digit",month:"2-digit"});document.querySelectorAll(".mission-tab").forEach(b=>b.classList.toggle("active",b.dataset.missionView===missionView));if(missionView==="base")renderMissionBase();if(missionView==="daily")renderMissionDaily();if(missionView==="weekly")renderMissionWeekly();if(missionView==="monthly")renderMissionMonthly()}function renderMissionBase(){const general=nextInstance(),daily=nextInstance("daily"),weekly=nextInstance("weekly"),monthly=nextInstance("monthly"),late=getInstances(-30,-1).filter(i=>i.late),st=dayStats(todayKey());$("missionView").innerHTML=`<div class="mission-lobby"><div class="card"><div class="section-head"><h2>Próxima</h2></div><div id="generalNext"></div></div><div class="card"><div class="section-head"><h2>Base hoje</h2></div><div class="stats-grid"><div class="stat"><strong>${st.done}/${st.totalDue}</strong><span>feitas</span></div><div class="stat"><strong>${st.percent}%</strong><span>progresso</span></div><div class="stat"><strong>${st.perfect?"⭐":"—"}</strong><span>dia perfeito</span></div></div></div></div><div class="card"><div class="panel-grid"><div class="panel"><div class="panel-title">☀️ Próxima diária</div><div id="nextDaily"></div></div><div class="panel"><div class="panel-title">📆 Próxima semanal</div><div id="nextWeekly"></div></div><div class="panel"><div class="panel-title">🗓️ Próxima mensal</div><div id="nextMonthly"></div></div></div></div><div class="card"><div class="section-head"><h2>Atrasadas</h2><span class="pill">${late.length}</span></div><div id="lateList" class="mission-list"></div></div>`;renderMini("generalNext",general,true);renderMini("nextDaily",daily);renderMini("nextWeekly",weekly);renderMini("nextMonthly",monthly);const lateList=$("lateList");if(!late.length)lateList.innerHTML=`<div class="empty">Nenhuma atrasada.</div>`;late.slice(0,6).forEach(i=>lateList.appendChild(missionCard(i)))}function renderMini(id,i,big=false){const box=$(id);if(!i){box.innerHTML=`<div class="empty">Nada pendente.</div>`;return}const t=i.task;box.innerHTML=`<div class="${big?'panel big':''}"><div class="mission-title">${catIcon[t.category]} ${t.text}</div>${tagHtml(t,i.dueDate)}<div class="actions"><button class="green small" onclick="completeTask('${t.id}','${i.dueDate}')">Feita</button><button class="small" onclick="missionView='${t.scheduleType}';renderMissions()">Ver</button></div></div>`}function renderMissionDaily(){const today=getInstances(0,0).filter(i=>i.task.scheduleType==="daily");$("missionView").innerHTML=`<div class="card"><div class="view-head"><h2>Diárias</h2><button class="small" onclick="openTaskModal('daily')">+ Nova diária</button></div><div id="dailyList" class="mission-list"></div></div>`;const list=$("dailyList");if(!today.length)list.innerHTML=`<div class="empty">Nenhuma diária para hoje.</div>`;today.forEach(i=>list.appendChild(missionCard(i)))}function renderMissionWeekly(){const base=parseDate(todayKey()),monday=addDays(base,-((base.getDay()+6)%7));$("missionView").innerHTML=`<div class="card"><div class="view-head"><h2>Semanais</h2><button class="small" onclick="openTaskModal('weekly')">+ Nova semanal</button></div><div class="week-board" id="weekBoard"></div></div>`;const board=$("weekBoard");for(let i=0;i<7;i++){const d=addDays(monday,i),key=dateKey(d),items=getInstances(0,7).filter(x=>x.dueDate===key&&x.task.scheduleType==="weekly"),col=document.createElement("div");col.className="week-day";col.innerHTML=`<strong>${weekFull[d.getDay()]}</strong><div class="mission-list"></div>`;const list=col.querySelector(".mission-list");if(!items.length)list.innerHTML=`<div class="empty">—</div>`;items.forEach(inst=>list.appendChild(missionCard(inst)));board.appendChild(col)}}function renderMissionMonthly(){const items=getInstances(0,45).filter(i=>i.task.scheduleType==="monthly"&&!i.done).sort((a,b)=>a.dueDate.localeCompare(b.dueDate));$("missionView").innerHTML=`<div class="card"><div class="view-head"><h2>Mensais</h2><button class="small" onclick="openTaskModal('monthly')">+ Nova mensal</button></div><div id="monthlyList" class="month-list"></div></div>`;const list=$("monthlyList");if(!items.length)list.innerHTML=`<div class="empty">Nenhuma mensal próxima.</div>`;items.forEach(i=>list.appendChild(missionCard(i)))}function openTaskModal(type,taskId=null){taskModalType=type;editingTaskId=taskId;const t=taskId?state.tasks.find(x=>x.id===taskId):null;$("taskModalTitle").textContent=t?"Editar missão":type==="daily"?"Nova diária":type==="weekly"?"Nova semanal":"Nova mensal";$("taskText").value=t?.text||"";$("taskCategory").value=t?.category||"casa";$("taskPerson").value=t?.person||"Mu";$("taskDifficulty").value=t?.difficulty||"facil";$("taskMinutes").value=t?.estimatedMinutes||5;$("taskInterval").value=t?.interval||1;$("taskMonthlyDay").value=t?.monthlyDay||1;$("taskInterval").style.display=type==="daily"?"block":"none";$("taskMonthlyDay").style.display=type==="monthly"?"block":"none";$("weekdayBox").style.display=type==="weekly"?"flex":"none";$("weekdayBox").innerHTML=[0,1,2,3,4,5,6].map(d=>`<button type="button" class="weekday ${(t?.weeklyDays||[]).includes(d)?"active":""}" data-day="${d}">${weekNames[d]}</button>`).join("");document.querySelectorAll(".weekday").forEach(b=>b.onclick=()=>b.classList.toggle("active"));$("taskModal").classList.add("open")}function saveTask(){const text=$("taskText").value.trim();if(!text)return alert("Digite o nome.");const data={text,category:$("taskCategory").value,person:$("taskPerson").value,difficulty:$("taskDifficulty").value,estimatedMinutes:Number($("taskMinutes").value||5),scheduleType:taskModalType,interval:Number($("taskInterval").value||1),monthlyDay:Number($("taskMonthlyDay").value||1),weeklyDays:[...document.querySelectorAll(".weekday.active")].map(b=>Number(b.dataset.day)),active:true,baseImpact:true};if(taskModalType==="weekly"&&!data.weeklyDays.length)return alert("Escolha ao menos um dia.");if(editingTaskId){Object.assign(state.tasks.find(x=>x.id===editingTaskId),data)}else state.tasks.push({...data,id:uid(),startDate:todayKey(),createdAt:new Date().toISOString()});$("taskModal").classList.remove("open");render()}
function personXp(p){return state.completions.filter(c=>c.person===p||c.person==="Ambos").reduce((s,c)=>s+c.xp/(c.person==="Ambos"?2:1),0)}function personCount(p){return state.completions.filter(c=>c.person===p||c.person==="Ambos").length}function baseXp(){return state.completions.filter(c=>c.baseImpact!==false).reduce((s,c)=>s+c.xp,0)+perfectDays()*50+completedGoals()*500}function beerForXp(xp){const idx=Math.min(beerLevels.length-1,Math.floor(xp/750));return{name:beerLevels[idx][0],title:beerLevels[idx][1],xp:Math.round(xp),progress:Math.round((xp%750)/750*100)}}function perfectDays(){return Object.values(state.daySnapshots).filter(s=>s.perfect).length}function completedGoals(){return state.goals.filter(g=>Number(g.current||0)>=Number(g.target||0)&&Number(g.target)>0).length}function streakFor(cat=null){let count=0,d=parseDate(todayKey());while(true){const key=dateKey(d),due=dueTasks(key).filter(t=>!cat||t.category===cat),doneCount=due.filter(t=>done(t.id,key)).length;if(due.length&&doneCount===due.length){count++;d=addDays(d,-1)}else break}return count}function homeStage(){const xp=baseXp();let idx=0;for(let i=0;i<homeStages.length;i++)if(xp>=homeStages[i][2])idx=i;return{idx,icon:homeStages[idx][0],name:homeStages[idx][1],next:homeStages[idx+1]?.[2]||homeStages[idx][2],min:homeStages[idx][2]}}function renderBase(){const mx=personXp("Mu"),jx=personXp("Ju"),bx=baseXp(),hs=homeStage();$("homeStageIcon").textContent=hs.icon;$("homeStageName").textContent=hs.name;$("homeStageDesc").textContent=`${Math.round(bx)} XP da Base • ${perfectDays()} dias perfeitos • ${completedGoals()} metas concluídas`;$("baseLevelPill").textContent=`Nível ${hs.idx+1}`;$("homeProgress").style.width=Math.min(100,Math.round((bx-hs.min)/Math.max(1,hs.next-hs.min)*100))+"%";$("dashMuBeer").textContent="🍺 "+beerForXp(mx).name;$("dashMuSummary").textContent=`${Math.round(mx)} XP • ${personCount("Mu")} missões`;$("dashJuBeer").textContent="🍺 "+beerForXp(jx).name;$("dashJuSummary").textContent=`${Math.round(jx)} XP • ${personCount("Ju")} missões`;$("dashBaseBeer").textContent="🍺 "+beerForXp(bx).name;$("dashBaseSummary").textContent=`${Math.round(bx)} XP • ${perfectDays()} dias perfeitos`;renderStreaks();renderProfiles();renderBeerCards();$("availableRewardCount").textContent="0 liberadas";$("availableRewards").innerHTML=`<div class="empty">Recompensas serão refinadas na próxima etapa.</div>`}function renderStreaks(){$("streakGrid").innerHTML=[["🏡","Base",null],["🏠","Casa","casa"],["🐾","Pets","pets"],["🧠","Saúde","saude"]].map(([i,n,c])=>`<div class="streak-card"><strong>${i} ${n}</strong><p>${streakFor(c)} dias seguidos</p></div>`).join("")}function renderProfiles(){$("profileGrid").innerHTML="";[["Mu",personXp("Mu"),"👨"],["Ju",personXp("Ju"),"👩"]].forEach(([name,xp,emoji])=>{const d=document.createElement("div");d.className="profile-card";d.innerHTML=`<div class="profile-avatar">${emoji}</div><h3>${name}</h3><p>${Math.round(xp)} XP</p><div class="avatar-items">${avatarItems.map(([icon,label,need])=>`<span class="item ${xp>=need?"":"locked"}">${icon} ${label}</span>`).join("")}</div>`;$("profileGrid").appendChild(d)})}function renderBeerCards(){const beers=[["Mu",personXp("Mu")],["Ju",personXp("Ju")],["Base",baseXp()]];$("beerCards").innerHTML="";beers.forEach(([name,xp])=>{const b=beerForXp(xp),d=document.createElement("div");d.className="dash-card";d.innerHTML=`<h3>${name}</h3><div class="dash-main">${b.name}</div><p>${b.xp} XP</p><div class="progress"><div class="progress-fill" style="width:${b.progress}%"></div></div>`;$("beerCards").appendChild(d)})}
function renderGoals(){const saved=state.goals.reduce((s,g)=>s+Number(g.current||0),0),target=state.goals.reduce((s,g)=>s+Number(g.target||0),0);$("cofrinhoTotal").textContent=money(saved);$("cofrinhoMissing").textContent=money(Math.max(0,target-saved));$("cofrinhoCount").textContent=state.goals.length;$("goalList").innerHTML="";if(!state.goals.length){$("goalList").innerHTML=`<div class="empty">Nenhuma meta.</div>`;return}state.goals.forEach(g=>{const pct=Math.min(100,g.target?Math.round(Number(g.current||0)/Number(g.target)*100):0),d=document.createElement("div");d.className="goal";d.innerHTML=`<div class="goal-top"><div><strong>${catIcon[g.category]||"🎯"} ${g.name}</strong><p>Faltam ${money(Math.max(0,Number(g.target)-Number(g.current||0)))}</p></div><strong>${money(g.current)} / ${money(g.target)}</strong></div><div class="progress"><div class="progress-fill" style="width:${pct}%"></div></div><div class="goal-actions"><input id="goal-${g.id}" type="number" step="0.01" placeholder="Adicionar R$"><button class="green small" onclick="addGoalMoney('${g.id}')">Adicionar</button><button class="red small" onclick="deleteGoal('${g.id}')">X</button></div>`;$("goalList").appendChild(d)})}function addGoal(){const name=$("goalName").value.trim(),target=Number($("goalTarget").value);if(!name||!target)return alert("Preencha nome e valor.");state.goals.push({id:uid(),name,category:$("goalCategory").value,target,current:0});$("goalModal").classList.remove("open");$("goalName").value="";$("goalTarget").value="";render()}function addGoalMoney(id){const v=Number($("goal-"+id).value);if(!v)return;const g=state.goals.find(x=>x.id===id);g.current=Number(g.current||0)+v;render()}function deleteGoal(id){if(confirm("Excluir meta?")){state.goals=state.goals.filter(g=>g.id!==id);render()}}
function renderHall(){const list=[],total=state.completions.length;[1,5,10,25,50,100,250,500].forEach(n=>list.push({cat:"Todas",name:`${n} missões feitas`,ok:total>=n,icon:"🏆"}));[1,3,7,15,30,60,100].forEach(n=>list.push({cat:"Dia Perfeito",name:`${n} dias perfeitos`,ok:perfectDays()>=n,icon:"⭐"}));["casa","pets","saude"].forEach(c=>[1,5,10,25,50,100].forEach(n=>list.push({cat:catLabel[c],name:`${n} em ${catLabel[c]}`,ok:state.completions.filter(x=>x.category===c).length>=n,icon:catIcon[c]})));const cats=["Todas","Casa","Pets","Saúde","Dia Perfeito"],un=list.filter(a=>a.ok).length;$("achievementBadge").textContent=un;$("trophyTabs").innerHTML="";cats.forEach(c=>{const b=document.createElement("button");b.className="small "+(trophyFilter===c?"active":"");b.textContent=c;b.onclick=()=>{trophyFilter=c;renderHall()};$("trophyTabs").appendChild(b)});$("achievementList").innerHTML="";(trophyFilter==="Todas"?list:list.filter(x=>x.cat===trophyFilter)).forEach(a=>{const d=document.createElement("div");d.className="trophy "+(a.ok?"":"locked");d.innerHTML=`<div class="trophy-icon">${a.ok?a.icon:"🔒"}</div><strong>${a.name}</strong>`;$("achievementList").appendChild(d)})}
function renderHistory(){const names=["Janeiro","Fevereiro","Março","Abril","Maio","Junho","Julho","Agosto","Setembro","Outubro","Novembro","Dezembro"],y=calendarMonth.getFullYear(),m=calendarMonth.getMonth();$("monthTitle").textContent=`${names[m]} de ${y}`;const first=new Date(y,m,1),days=new Date(y,m+1,0).getDate();let off=first.getDay()-1;if(off<0)off=6;$("calendarDays").innerHTML="";for(let i=0;i<off;i++){const e=document.createElement("div");e.className="day empty";$("calendarDays").appendChild(e)}for(let d=1;d<=days;d++){const k=dateKey(new Date(y,m,d)),s=dayStats(k),el=document.createElement("div");el.className="day "+(k===todayKey()?"today":"");el.innerHTML=`<strong>${d}</strong><div class="day-score">${s.done}/${s.totalDue}</div><div class="day-score">${s.percent}% ${s.perfect?"⭐":""}</div>`;$("calendarDays").appendChild(el)}$("completedList").innerHTML="";state.completions.slice().sort((a,b)=>b.completedAt.localeCompare(a.completedAt)).slice(0,50).forEach(c=>{const e=document.createElement("div");e.className="entry";e.innerHTML=`<div><strong>${catIcon[c.category]} ${c.taskText}</strong><p>${c.person} • ${new Date(c.completedAt).toLocaleString("pt-BR")} • ${c.xp} XP</p></div>`;$("completedList").appendChild(e)})}
function render(sync=true){renderMissions();renderBase();renderGoals();renderHall();renderHistory();save(sync)}function setup(){const theme=localStorage.getItem("bf_theme")||"light";document.documentElement.dataset.theme=theme;$("themeToggle").textContent=theme==="dark"?"☀️ Tema":"🌙 Tema";$("themeToggle").onclick=()=>{const n=document.documentElement.dataset.theme==="dark"?"light":"dark";document.documentElement.dataset.theme=n;localStorage.setItem("bf_theme",n);$("themeToggle").textContent=n==="dark"?"☀️ Tema":"🌙 Tema"};document.querySelectorAll(".nav-btn").forEach(b=>b.onclick=()=>{document.querySelectorAll(".screen").forEach(s=>s.classList.remove("active"));$("screen-"+b.dataset.screen).classList.add("active");document.querySelectorAll(".nav-btn").forEach(x=>x.classList.remove("active"));b.classList.add("active")});document.querySelectorAll(".mission-tab").forEach(b=>b.onclick=()=>{missionView=b.dataset.missionView;renderMissions()});$("saveTaskBtn").onclick=saveTask;$("closeTaskModal").onclick=()=>$("taskModal").classList.remove("open");$("openGoalBtn").onclick=()=>$("goalModal").classList.add("open");$("closeGoalBtn").onclick=()=>$("goalModal").classList.remove("open");$("saveGoalBtn").onclick=addGoal;$("prevMonthBtn").onclick=()=>{calendarMonth.setMonth(calendarMonth.getMonth()-1);render(false)};$("nextMonthBtn").onclick=()=>{calendarMonth.setMonth(calendarMonth.getMonth()+1);render(false)}}migrate();setup();if("serviceWorker"in navigator)navigator.serviceWorker.register("sw.js").catch(()=>{});render(false);initSupabase().then(()=>render(false));
